# 《智愿 AI 报考平台》详细设计说明书

> 文档编号：LLD-ZHIYUAN
> 对应版本：`[tag/commit]`
> 注意：接口、类、字段必须从提交版本代码生成/核验。

## 1. 设计约定

- Java 包名：`com.zhiyuan.college`；
- 前端：Vue 3 Composition API + Vue Router；
- API：JSON，服务端鉴权；
- 枚举、错误码、时区、空值和分页约定：`[填写]`。

## 2. 后端分层与依赖

为 Controller → Service → Mapper → Entity/DTO 绘制依赖图。列出禁止依赖：模型客户端不得直接操作 Mapper；Agent 不得绕过业务 Service；前端权限判断不替代服务端鉴权。

## 3. 推荐算法详细设计

### 3.1 输入规范化

| 字段 | 类型 | 范围/枚举 | 缺失处理 | 错误 |
| --- | --- | --- | --- | --- |
| score | Integer | `[实际范围]` | 拒绝/追问 | 400 |
| province | String | 元数据列表 | 拒绝/追问 | 400 |
| subjectType | Enum | `[实际枚举]` | 拒绝/追问 | 400 |
| recommendationMode | Enum | SCHOOL_FIRST/MAJOR_FIRST | 默认值 | 400 |
| majorKeyword | String | `[实际长度]` | 专业优先必填 | 400 |

### 3.2 候选查询

分别描述院校优先、专业优先使用的 Mapper 查询、最新年份策略、重复去除、空结果和索引。

### 3.3 概率与分档

从 `RecommendationPolicyService` 抄录当前提交版本的真实公式/阈值，并解释：

- `scoreGap`、`rankGap` 的方向；
- 位次概率与分数概率如何融合；
- 最低概率过滤；
- 冲/稳/保区间；
- 排序与每组上限；
- 为什么这只是历史数据辅助评分，不是统计意义上的真实录取概率。

不要沿用旧文档中的建议阈值，必须以代码与配置为准。

### 3.4 解释生成与降级

说明结构化 `matchReasons`、模板解释和模型润色的优先级；模型失败时不得删除规则结果。

## 4. Agent 编排详细设计

### 4.1 当前白名单工具

| 工具 | 参数 | 读/写 | 后端服务 | 校验/确认 |
| --- | --- | --- | --- | --- |
| getUserProfile | 无 | 读 | `[填写]` | 本人 |
| getCurrentPlan | planId/上下文 `[核验]` | 读 | `[填写]` | 本人 |
| getSchoolDetail | selectionIndex | 读 | `[填写]` | 1—6 `[核验]` |
| getSchoolDetailByName | universityName | 读 | `[填写]` | 2—40 `[核验]` |
| recommendSchools | 无/画像 | 读 | RecommendationService | 画像完整 |
| recommendMajors | majorKeyword | 读 | RecommendationService | 最长 20 `[核验]` |
| addPlanItem | selectionIndex | 写 | ApplicationPlanService | 目标方案 |
| removePlanItem | selectionIndex | 写 | ApplicationPlanService | 二次确认 |
| savePlan | planName | 写 | ApplicationPlanService | 2—30 `[核验]` |

### 4.2 工具决策

描述 AI JSON 决策、本地规则决策、非法 JSON、未知工具、非法参数和模型异常的分支；不记录模型完整思维链，只记录必要决策事实。

### 4.3 比赛版状态机（落地后填写）

| 状态 | 进入条件 | 允许动作 | 退出条件 | 持久化字段 | 超时/恢复 |
| --- | --- | --- | --- | --- | --- |
| REQUIREMENTS | 新任务 | 补充/修改条件 | 必填条件齐 | `[填写]` | `[填写]` |
| CANDIDATES | 条件齐 | 生成候选 | 候选完成 | `[填写]` | `[填写]` |
| CONSTRAINT_CHECK | 有候选 | 校验/请求取舍 | 无硬冲突 | `[填写]` | `[填写]` |
| WHAT_IF | 有草案 | 修改变量/对比 | 用户接受 | `[填写]` | `[填写]` |
| AWAIT_CONFIRMATION | 待写入 | 确认/取消 | 保存或取消 | `[填写]` | `[填写]` |

如果状态机尚未实现，本节标题必须标注“设计提案”，不得写成提交版本能力。

### 4.4 幂等与并发

说明重复消息、重复确认、并行浏览器标签、超时重试和相同 requestId 的处理；无法保证的部分列为限制。

## 5. 志愿方案设计

描述 `resultJson` 的结构、冲稳保分组、条目唯一键、当前草稿、命名方案、增删与保存。给出脱敏 JSON 示例，不使用真实用户数据。

## 6. 异步任务设计

说明 PENDING/RUNNING/SUCCEEDED/FAILED `[以实际枚举核验]`、消息字段、生产/消费、重试次数、错误信息、同步回退和任务查询。

## 7. 安全详细设计

### 7.1 鉴权链

请求 → JWT Filter → UserContext → URL/角色规则 → Controller → 资源归属检查。

### 7.2 Agent 安全边界

- 白名单注册；
- 参数长度/范围；
- 资源归属；
- 删除确认；
- 工具错误分类；
- prompt injection/文档内容只作数据 `[如实现再填]`。

### 7.3 密钥与配置

列出变量名但不写值：DB、JWT、QWEN、Redis、RocketMQ。说明 dev/prod/competition profile 的差异。

## 8. 前端详细设计

| 路由 | 组件 | 权限 | 状态所有者 | 刷新恢复 | 空/错/加载状态 |
| --- | --- | --- | --- | --- | --- |
| `/login` | LoginView | 匿名 | App/workspace `[核验]` | 无 | `[填写]` |
| `/recommend` | RecommendationView | USER | `[填写]` | keepAlive | `[填写]` |
| `/agent` | AgentView/AgentWorkspace | USER | `[填写]` | 服务端会话 | `[填写]` |
| `/history` | HistoryRecordsView | USER | `[填写]` | API | `[填写]` |
| `/plans` | PlansView | USER | `[填写]` | API | `[填写]` |
| `/admin` | AdminView | ADMIN | `[填写]` | query/API | `[填写]` |

## 9. 接口详细表

每个公开接口填写：方法、路径、请求 DTO、响应 DTO、鉴权、校验、错误、幂等性、测试编号。推荐从 OpenAPI 导出后人工补充业务语义。

## 10. 错误与日志

| 错误类别 | HTTP/业务码 | 用户信息 | 日志字段 | 是否可重试 |
| --- | --- | --- | --- | --- |
| 参数错误 | 400 | 字段级说明 | traceId/字段 | 否 |
| 未认证/越权 | 401/403 | 登录或权限提示 | userId/route | 视情况 |
| 模型失败 | `[核验]` | 已降级/稍后重试 | provider/latency/category | 是 |
| 数据缺失 | `[核验]` | 明确缺失范围 | query/source | 否 |

日志不得记录密码、完整 token、模型 key、真实身份证号或不必要的完整提示词。

## 11. 测试设计映射

列出单元、控制器、集成、离线评测、浏览器、部署和故障注入；给出命令、环境、样本数、通过标准和报告位置。

## 12. 已知限制

如实列出省份/年份覆盖、概率解释限制、外部模型依赖、并发上限、移动端/浏览器范围和未实现的比赛版能力。
