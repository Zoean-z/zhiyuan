# 《智愿 AI 报考平台》数据库设计说明书

> 文档编号：DBD-ZHIYUAN
> Schema 版本/提交：`[填写]`
> 数据快照日期：`[填写]`

## 1. 设计目标

说明基础数据、录取事实、用户业务、异步任务和 Agent 轨迹分离的原因，以及 MySQL 8/H2 测试库边界。

## 2. 数据库概览

当前代码基线包含 12 张表，定稿前按 `sql/schema.sql` 重新统计：

| 分组 | 表 |
| --- | --- |
| 院校与录取事实 | `university`、`major`、`admission_cutoff`、`major_admission_cutoff`、`score_rank_mapping` |
| 用户与业务结果 | `users`、`recommendation_log`、`application_plan` |
| 异步与 AI 解析 | `recommendation_task`、`ai_parse_log` |
| Agent 会话 | `agent_conversation`、`agent_message` |

## 3. ER 图

用数据库工具从提交版本 schema 生成 ER 图，再人工标注：主键、外键、1:N、可空关系和 JSON/TEXT 字段。不要手画与真实外键不一致的图。

## 4. 表结构

每张表复制以下小节。

### 4.x `[table_name]` — `[中文名]`

**职责：** `[一句话]`
**数据来源：** `[用户输入/官方数据/系统生成]`
**生命周期：** `[创建、更新、归档/删除]`

| 字段 | 类型 | 可空 | 默认 | 键/索引 | 业务含义 | 脱敏/约束 |
| --- | --- | --- | --- | --- | --- | --- |
| id | BIGINT | 否 | 自增 | PK | 主键 | 不在公开材料展示完整 ID |
| `[字段]` | `[类型]` | `[是/否]` | `[值]` | `[索引]` | `[说明]` | `[规则]` |

**关键约束：** `[唯一键、外键、检查规则]`
**典型查询：** `[查询条件、排序、预计数据量]`
**索引理由：** `[如何支持查询，避免“为了性能”空话]`

## 5. 关键表说明要点

### 5.1 录取事实

- `admission_cutoff` 与 `major_admission_cutoff` 的粒度差异；
- year/province/subjectType/minRank/cutoffScore 的定义；
- 多年份数据如何选取“最新”或参与波动计算；
- 同一院校/专业重复数据的唯一性策略。

### 5.2 位次映射

说明分数到位次映射的数据年份、同分处理和缺失回退；禁止把不同年份/科类混用。

### 5.3 方案 JSON

解释 `application_plan.result_json` 的版本、冲稳保数组、兼容策略和迁移风险。附脱敏示例与 JSON Schema `[如有]`。

### 5.4 Agent 消息

解释 role、messageType、toolName、content、payloadJson 与时间顺序；说明为什么只保存必要轨迹、不保存密钥和完整隐私信息。

## 6. 数据来源与血缘

| 数据集 | 原始来源 URL/文件 | 年份/省份/科类 | 获取日期 | 授权/使用边界 | 清洗规则 | 导入脚本 | 抽查结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `[填写]` | `[填写]` | `[填写]` | `[填写]` | `[填写]` | `[填写]` | `[填写]` | `[n/N]` |

附一条完整血缘：原始记录 → 清洗前 → 清洗后 → 表行 → 推荐响应字段。

## 7. 数据质量

| 规则 | 检查 SQL/脚本 | 通过标准 | 实际结果 | 日期 |
| --- | --- | --- | --- | --- |
| 年份合法 | `[填写]` | 无异常年份 | `[待测]` | `[填写]` |
| 分数/位次范围 | `[填写]` | 0 异常或有解释 | `[待测]` | `[填写]` |
| 外键完整 | `[填写]` | 0 孤儿记录 | `[待测]` | `[填写]` |
| 重复事实 | `[填写]` | 符合唯一粒度 | `[待测]` | `[填写]` |

## 8. 安全与隐私

- 密码哈希算法与字段不出响应；
- 最小化收集画像，不收身份证/联系方式 `[以实际核验]`；
- 日志与导出脱敏；
- 管理端权限；
- 测试/演示数据与真实数据隔离；
- 保留/删除策略 `[填写]`。

## 9. 初始化、迁移与回滚

描述 `sql/schema.sql` 的幂等建表/兼容字段、`data.sql` 是否自动执行、生产账号 DDL 权限、备份恢复和失败回滚。严禁建议直接删除比赛数据卷。

## 10. 容量与性能

填写真实行数、索引大小、典型查询 EXPLAIN、P50/P95；没有测量就不写“百万级”“高并发”。

## 11. 备份与恢复演练

| 项目 | 命令/工具 | 恢复点 | 演练日期 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 比赛数据快照 | `[填写]` | `[版本]` | `[日期]` | `[结果]` | `[路径]` |

## 12. 数据字典附录

附枚举、状态码、JSON 字段、索引清单、外键清单和 schema SHA-256。
