# Vite development-server proxy. Change this when the backend is exposed on a
# different host or port; the browser will still call the relative /api path.
VITE_API_PROXY_TARGET=http://127.0.0.1:8080
